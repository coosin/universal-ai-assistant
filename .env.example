## Universal AI Assistant 本地环境变量示例
## 复制为 .env（不要提交到仓库）

# OpenRouter（可选）
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 代理（国内使用 Telegram / OpenRouter 常用）
# NO_PROXY=localhost,127.0.0.1,::1
#
# 方案 A：本机 Clash（推荐，长期可靠。见 docs/CLASH_SETUP.md）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
#
# 方案 B：本机 WARP（海外可用；国内可能不稳定）
# HTTP_PROXY=socks5://127.0.0.1:40000
# HTTPS_PROXY=socks5://127.0.0.1:40000
#
# 方案 C：临时代理（如 myhome 上的 Clash，仅作临时）
# 注意：WSL2 下 Windows 的 localhost 在 WSL 里不可用，需用宿主机 IP
# HTTP_PROXY=http://192.168.1.2:7897
# HTTPS_PROXY=http://192.168.1.2:7897
#
# Clash 订阅自动更新（可选，用于 scripts/clash_update_subscription.sh 与 cron）
# CLASH_SUBSCRIPTION_URL=https://你的订阅链接
#
# home→myhome 自动同步（sync_home_to_myhome.sh）
# MYHOME_HOST=myhome   # 或 cool@192.168.1.2，需在 home 的 ~/.ssh/config 配置并免密

