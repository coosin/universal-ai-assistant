## Universal AI Assistant 本地环境变量示例
## 复制为 .env（不要提交到仓库）

# OpenRouter（可选）
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 代理（国内使用 Telegram / OpenRouter 常用）
# 注意：WSL2 NAT 模式下，Windows 的 localhost 代理端口在 WSL 里不可直接用
# 需要把 127.0.0.1 换成 Windows 宿主机 IP（通常可从 /etc/resolv.conf 的 nameserver 获取）
#
# 例：HTTP_PROXY=http://172.22.224.1:7890
# HTTP_PROXY=http://<windows-host-ip>:7890
# HTTPS_PROXY=http://<windows-host-ip>:7890
# NO_PROXY=localhost,127.0.0.1,::1

