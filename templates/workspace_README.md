# OpenClaw 工作区说明

将本仓库 `templates/` 下内容按需复制到 `~/.openclaw/workspace/` 使用。

- `CLAUDE.md` -> `~/.openclaw/workspace/coding/CLAUDE.md` 供代码 Agent 读取项目约定。
- Skills 已由 `install.sh` 部署到 `~/.openclaw/workspace/skills/`。
